/* groovylint-disable DuplicateStringLiteral, NoDef */

pipeline {
    agent any
    environment {
        DOCKER_IMAGE_TAG = "${env.BRANCH_NAME}-${env.BUILD_NUMBER}"
    }
    stages {
        stage('build app'){
            steps {

            }
        }
        stage('launch app'){
            steps {
                
            }
        }
        stage('ru tests') {
            steps {
                
            }
        }
    }
    post {
        always {
            
        }

    }
}
